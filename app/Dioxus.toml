# Dioxus 0.7 build config.
#
# Android is built with `dx`, not the cargo-mobile2 scaffolding in gen/,
# dioxus-desktop exports its own JNI trampoline. See src/lib.rs.
[application]
name = "two_khz"

[web.app]
title = "2kHz"

[bundle]
identifier = "fr.glargh.twokhz"
publisher = "two_khz"
